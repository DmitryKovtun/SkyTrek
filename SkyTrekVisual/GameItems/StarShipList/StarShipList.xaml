<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:SkyTrekVisual.GameItems.StarShipList">


    <Style x:Key="StarShipListBoxItemStyle" TargetType="{x:Type ListBoxItem}">
        <Setter Property="Width" Value="128"/>
        <Setter Property="Height" Value="128"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                    <Grid x:Name="mainLayer" Background="LightGray" Margin="2">
                        <Border x:Name="mainBorder" CornerRadius="4" BorderThickness="2">
                            <Image Stretch="Uniform" Margin="4"/>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="mainBorder" Property="BorderBrush" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style x:Key="StarShipListBoxStyle" TargetType="{x:Type ListBox}">
        <Setter Property="ItemContainerStyle" Value="{StaticResource StarShipListBoxItemStyle}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBox}">
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--<ListBox Grid.Row="1" SelectionMode="Extended"  SelectedItem="{Binding SelectedItem}"  ItemContainerStyle="{StaticResource BackupListBoxItemStyle}" ItemsSource="{Binding BList}">
        <i:Interaction.Behaviors>
            <behaviors:ListBoxSelectedItemsBehavior SelectedItems="{Binding SelectedItems, Mode=OneWayToSource}" />
        </i:Interaction.Behaviors>
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="MouseDoubleClick">
                <i:InvokeCommandAction Command="{Binding OpenBackupItemWindowCommand}"/>
            </i:EventTrigger>
        </i:Interaction.Triggers>
        </ListBox> -->
    
    
    







</ResourceDictionary>